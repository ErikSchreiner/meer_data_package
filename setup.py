from setuptools import setup, find_packages

setup(
    name='meer_data_analysis',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)