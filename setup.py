from setuptools import setup, find_packages

setup(
    name='meerDataAnalysis',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
)